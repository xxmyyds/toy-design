import * as components from '@toy-design/toy-components'
import { makeInstall } from '@toy-design/toy-tools'
// console.log()

const toyDesign = makeInstall(Object.values(components))
export default toyDesign
